# مستندات سیستم دانش‌آموزان (Student Profiles)

## هدف
هر دانش‌آموز یک صفحه اختصاصی دارد که تمام تحلیل‌های روزانه مربوط به او در طول زمان در آن جمع می‌شود. این امکان منتورینگ واقعی و پیگیری روند رشد هر فرد را فراهم می‌کند.

---

## ساختار URLها

| مسیر | توضیح |
|------|-------|
| `/students` | لیست تمام دانش‌آموزان |
| `/students/[slug]` | صفحه اختصاصی هر دانش‌آموز |

مثال:
- `https://autokad.vercel.app/students/تینا-توسلی-منش`
- `https://autokad.vercel.app/students/عسل-کاظمی`

---

## نحوه کار

### ۱. ربات هرمس هنگام ارسال تحلیل
در `visual_json` فیلد `students` یک آرایه از آبجکت‌های زیر است:

```json
{
  "name": "تینا توسلی منش",
  "productivity_score": 8,
  "status": "green",
  "yesterday_summary": "...",
  "today_plan": "...",
  "obstacles": ""
}
```

**فیلد `name` دقیقاً باید با نام موجود در فایل `src/lib/students.ts` مطابقت داشته باشد.**

### ۲. ذخیره در دیتابیس
- ربات هرمس `visual_json` کامل را به `POST /api/hermes/submit-analysis` ارسال می‌کند
- پلتفرم آن را در ستون `visual_json` جدول `reports` ذخیره می‌کند
- API صفحه دانش‌آموز (`GET /api/students/[name]`) تمام رکوردها را می‌خواند و فقط تحلیل‌های مربوط به آن دانش‌آموز را فیلتر می‌کند

### ۳. نمایش در صفحه دانش‌آموز
صفحه `/students/[slug]`:
- نام دانش‌آموز را نمایش می‌دهد
- تمام گزارش‌های مربوط به او را در یک جدول زمانی نشان می‌دهد
- هر ردیف شامل: تاریخ، نمره بهره‌وری، وضعیت، خلاصه دیروز، برنامه امروز، موانع
- کلیک روی تاریخ، کاربر را به صفحه آن گزارش هدایت می‌کند

### ۴. لیست دانش‌آموزان
صفحه `/students`:
- کارت هر دانش‌آموز را با نام و تعداد گزارش‌هایش نمایش می‌دهد
- آخرین وضعیت و بهره‌وری را نشان می‌دهد
- قابلیت جستجو دارد

---

## نکات مهم برای ربات هرمس

1. **نام دانش‌آموز (`name`) در ارسال تحلیل‌ها باید با `src/lib/students.ts` یکی باشد** — در غیر این صورت تحلیل او در صفحه شخصی‌اش نمایش داده نمی‌شود.
2. **فیلدهای الزامی در هر آبجکت دانش‌آموز:**
   - `name` (string) — نام دقیق
   - `productivity_score` (number 1-10) — نمره بهره‌وری
   - `status` ("green" | "yellow" | "red") — وضعیت
   - `yesterday_summary` (string) — خلاصه دیروز
   - `today_plan` (string) — برنامه امروز
   - `obstacles` (string) — موانع (می‌تواند خالی باشد)

---

## اضافه کردن دانش‌آموز جدید

۱. اسم را به آرایه `students` در فایل `src/lib/students.ts` اضافه کن
۲. یک slug منحصربه‌فرد (با خط تیره بین کلمات) برایش بساز
۳. commit and push — ورسل خودکار دپلوی می‌کند
۴. به ربات هرمس بگو اسم دقیق را در تحلیل‌هایش استفاده کند
